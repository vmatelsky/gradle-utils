to use custom plugin in consumer run following command in cusotm plugin directory:

./gradlew uploadArchives

this command build custom plugin and upload it in repository.
Concumer is configured to use the same repository.
